services:
  backend:
    build: .
    environment:
      - OPENAI_API_KEY=${OPENAI_API_KEY}
      - QDRANT_API_KEY=${QDRANT_API_KEY}
    ports:
      - 8000:8000
    volumes:
      - .:/app

  frontend:
    build: ./frontend
    ports:
      - 80:80
    depends_on:
      - backend
